# LexiNote React App

A minimal React app using Vite that renders "Hello World" from src/App.jsx.

## Setup

Ensure Node.js 18+ is installed.

```bash
npm install
npm run dev
```

Then open the printed local URL (typically http://localhost:5173).

## Build & Preview

```bash
npm run build
npm run preview
```